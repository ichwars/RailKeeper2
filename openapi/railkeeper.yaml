openapi: 3.1.0
info:
  title: RailKeeper2 API
  version: 0.1.0
servers:
  - url: /api/v1
tags:
  - name: Setup
  - name: Auth
  - name: Vehicles
  - name: InventoryNumbers
  - name: MasterData
  - name: ArticleSearch
  - name: Backup
  - name: Audit
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: rk_session
  schemas:
    Problem:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
        message:
          type: string
    SetupStatus:
      type: object
      required: [setupRequired]
      properties:
        setupRequired:
          type: boolean
    CreateAdminRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
          minLength: 3
        password:
          type: string
          minLength: 12
    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
    Session:
      type: object
      required: [username, roles, csrfToken]
      properties:
        username:
          type: string
        roles:
          type: array
          items:
            type: string
        csrfToken:
          type: string
    Vehicle:
      type: object
      required: [id, inventoryNumber, manufacturer, name, gauge, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        inventoryNumber:
          type: string
        manufacturer:
          type: string
        articleNumber:
          type: string
        articleSourceUrl:
          type: string
        name:
          type: string
        gauge:
          type: string
        epoch:
          type: string
        railwayCompany:
          type: string
        category:
          type: string
        gattung:
          type: string
        description:
          type: string
        series:
          type: string
        vehicleNumber:
          type: string
        digital:
          type: boolean
        digitalDecoderNumber:
          type: string
        dtDecoder:
          type: boolean
        dtDecoderNumber:
          type: string
        exhibitionReady:
          type: boolean
        abcBrakes:
          type: boolean
        ean:
          type: string
        productionPeriod:
          type: string
        listPrice:
          type: string
        lengthMm:
          type: string
        weightG:
          type: string
        color:
          type: string
        lettering:
          type: string
        load:
          type: string
        interior:
          type: string
        axles:
          type: string
        axleCount:
          type: string
        tractionTireCount:
          type: string
        wheelset:
          type: string
        couplingSame:
          type: boolean
        couplingFront:
          type: string
        couplingRear:
          type: string
        powerPickup:
          type: string
        adapter:
          type: string
        driveEnabled:
          type: boolean
        driveDescription:
          type: string
        headlightsEnabled:
          type: boolean
        headlightsDescription:
          type: string
        lightingEnabled:
          type: boolean
        lightingDescription:
          type: string
        soundGeneratorEnabled:
          type: boolean
        soundGeneratorDescription:
          type: string
        smokeGeneratorEnabled:
          type: boolean
        smokeGeneratorDescription:
          type: string
        additionalInfo:
          type: string
        qrCodeEnabled:
          type: boolean
        images:
          type: array
          items:
            $ref: "#/components/schemas/VehicleImage"
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/VehicleAttachment"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateVehicleRequest:
      type: object
      required: [manufacturer, name, gauge]
      properties:
        inventoryNumber:
          type: string
        manufacturer:
          type: string
        articleNumber:
          type: string
        articleSourceUrl:
          type: string
        name:
          type: string
        gauge:
          type: string
        epoch:
          type: string
        railwayCompany:
          type: string
        category:
          type: string
        gattung:
          type: string
        description:
          type: string
        series:
          type: string
        vehicleNumber:
          type: string
        digital:
          type: boolean
        digitalDecoderNumber:
          type: string
        dtDecoder:
          type: boolean
        dtDecoderNumber:
          type: string
        exhibitionReady:
          type: boolean
        abcBrakes:
          type: boolean
        ean:
          type: string
        productionPeriod:
          type: string
        listPrice:
          type: string
        lengthMm:
          type: string
        weightG:
          type: string
        color:
          type: string
        lettering:
          type: string
        load:
          type: string
        interior:
          type: string
        axles:
          type: string
        axleCount:
          type: string
        tractionTireCount:
          type: string
        wheelset:
          type: string
        couplingSame:
          type: boolean
        couplingFront:
          type: string
        couplingRear:
          type: string
        powerPickup:
          type: string
        adapter:
          type: string
        driveEnabled:
          type: boolean
        driveDescription:
          type: string
        headlightsEnabled:
          type: boolean
        headlightsDescription:
          type: string
        lightingEnabled:
          type: boolean
        lightingDescription:
          type: string
        soundGeneratorEnabled:
          type: boolean
        soundGeneratorDescription:
          type: string
        smokeGeneratorEnabled:
          type: boolean
        smokeGeneratorDescription:
          type: string
        additionalInfo:
          type: string
        qrCodeEnabled:
          type: boolean
        images:
          type: array
          items:
            $ref: "#/components/schemas/VehicleImageInput"
    VehicleImage:
      type: object
      required: [id, vehicleId, url, isPrimary, sortOrder, createdAt]
      properties:
        id:
          type: string
        vehicleId:
          type: string
        url:
          type: string
        title:
          type: string
        sourceUrl:
          type: string
        isPrimary:
          type: boolean
        sortOrder:
          type: integer
        createdAt:
          type: string
          format: date-time
    VehicleImageInput:
      type: object
      required: [url]
      properties:
        url:
          type: string
        title:
          type: string
        sourceUrl:
          type: string
        isPrimary:
          type: boolean
        sortOrder:
          type: integer
    VehicleAttachment:
      type: object
      required: [id, vehicleId, fileName, originalName, sizeBytes, createdAt, updatedAt]
      properties:
        id:
          type: string
        vehicleId:
          type: string
        fileName:
          type: string
        originalName:
          type: string
        description:
          type: string
        category:
          type: string
        mimeType:
          type: string
        sizeBytes:
          type: integer
          format: int64
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    VehicleAttachmentUpdateRequest:
      type: object
      properties:
        description:
          type: string
        category:
          type: string
    MasterDataEntry:
      type: object
      required: [id, type, key, label, active, sortOrder, metadata, createdAt, updatedAt]
      properties:
        id:
          type: string
        type:
          type: string
        key:
          type: string
        label:
          type: string
        active:
          type: boolean
        sortOrder:
          type: integer
        sourceUrl:
          type: string
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MasterDataInput:
      type: object
      required: [label]
      properties:
        key:
          type: string
        label:
          type: string
        active:
          type: boolean
        sortOrder:
          type: integer
        sourceUrl:
          type: string
        metadata:
          type: object
          additionalProperties: true
    MasterDataRelation:
      type: object
      required: [id, parentType, parentKey, childType, childKey, sortOrder]
      properties:
        id:
          type: string
        parentType:
          type: string
        parentKey:
          type: string
        childType:
          type: string
        childKey:
          type: string
        sortOrder:
          type: integer
    MasterDataDocument:
      type: object
      required: [format, version, createdAt, entries, relations]
      properties:
        format:
          type: string
          example: railkeeper2-master-data
        version:
          type: integer
          example: 1
        createdAt:
          type: string
          format: date-time
        entries:
          type: object
          additionalProperties:
            type: array
            items:
              $ref: "#/components/schemas/MasterDataEntry"
        relations:
          type: array
          items:
            $ref: "#/components/schemas/MasterDataRelation"
    MasterDataImportResult:
      type: object
      required: [importedTypes, importedEntries, importedRelations]
      properties:
        importedTypes:
          type: integer
        importedEntries:
          type: integer
        importedRelations:
          type: integer
    InventoryNumberScheme:
      type: object
      required: [id, category, prefix, nextNumber, padding, active, preview, createdAt, updatedAt]
      properties:
        id:
          type: string
        category:
          type: string
        prefix:
          type: string
        nextNumber:
          type: integer
          minimum: 1
        padding:
          type: integer
          minimum: 1
          maximum: 12
        active:
          type: boolean
        preview:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    InventoryNumberSchemeInput:
      type: object
      required: [prefix, nextNumber, padding, active]
      properties:
        prefix:
          type: string
        nextNumber:
          type: integer
          minimum: 1
        padding:
          type: integer
          minimum: 1
          maximum: 12
        active:
          type: boolean
    ArticleSearchInput:
      type: object
      properties:
        manufacturer:
          type: string
        articleNumber:
          type: string
        name:
          type: string
        gauge:
          type: string
        fields:
          type: object
          additionalProperties:
            type: string
    ArticleSearchField:
      type: object
      required: [label, value, confidence]
      properties:
        label:
          type: string
        value:
          type: string
        confidence:
          type: integer
    ArticleSearchImage:
      type: object
      required: [url, title, source]
      properties:
        url:
          type: string
        title:
          type: string
        source:
          type: string
    ArticleSearchResult:
      type: object
      required: [source, title, url, snippet, score, fields]
      properties:
        source:
          type: string
        title:
          type: string
        url:
          type: string
        snippet:
          type: string
        score:
          type: integer
        fields:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ArticleSearchField"
        images:
          type: array
          items:
            $ref: "#/components/schemas/ArticleSearchImage"
        conflicts:
          type: array
          items:
            type: string
    ArticleSearchResponse:
      type: object
      required: [query, results]
      properties:
        query:
          type: string
        results:
          type: array
          items:
            $ref: "#/components/schemas/ArticleSearchResult"
    VehicleImportPreview:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          description: Tabellenzeilen aus der ersten Arbeitsmappe. Die erste Zeile enthält die Header.
          items:
            type: array
            items:
              type: string
    BackupImportResult:
      type: object
      required: [restoredTables, restoredRows, restoredFiles]
      properties:
        restoredTables:
          type: integer
        restoredRows:
          type: integer
        restoredFiles:
          type: integer
paths:
  /setup/status:
    get:
      tags: [Setup]
      security: []
      responses:
        "200":
          description: Setup state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetupStatus"
  /setup/admin:
    post:
      tags: [Setup]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAdminRequest"
      responses:
        "201":
          description: Admin user created
        "400":
          description: Weak input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "409":
          description: Setup already completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /auth/session:
    get:
      tags: [Auth]
      responses:
        "200":
          description: Current session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "401":
          description: Not logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /auth/login:
    post:
      tags: [Auth]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /auth/logout:
    post:
      tags: [Auth]
      responses:
        "204":
          description: Logged out
  /vehicles:
    get:
      tags: [Vehicles]
      parameters:
        - name: q
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Vehicle list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Vehicle"
    post:
      tags: [Vehicles]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVehicleRequest"
      responses:
        "201":
          description: Vehicle created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Invalid vehicle data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "403":
          description: Insufficient role
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /vehicle-import/preview:
    post:
      tags: [Vehicles]
      summary: Preview a vehicle import file
      description: Reads a supported import file and returns table rows for client-side review. Currently supports XLSX and ODS.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Parsed table rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleImportPreview"
        "400":
          description: Invalid or unsupported import file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "403":
          description: Insufficient role
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /vehicles/{id}:
    get:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Vehicle detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
    put:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVehicleRequest"
      responses:
        "200":
          description: Vehicle updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Invalid vehicle data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Vehicle deleted
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /vehicles/{id}/attachments:
    post:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                category:
                  type: string
                description:
                  type: string
      responses:
        "201":
          description: Attachment uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleAttachment"
        "400":
          description: Invalid attachment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /vehicles/{id}/attachments/{attachmentID}:
    put:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: attachmentID
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VehicleAttachmentUpdateRequest"
      responses:
        "200":
          description: Attachment updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleAttachment"
        "404":
          description: Attachment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: attachmentID
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Attachment deleted
        "404":
          description: Attachment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /vehicles/{id}/attachments/{attachmentID}/download:
    get:
      tags: [Vehicles]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: attachmentID
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Attachment file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "404":
          description: Attachment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /inventory-number-schemes:
    get:
      tags: [InventoryNumbers]
      responses:
        "200":
          description: Inventory number schemes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryNumberScheme"
  /inventory-number-schemes/{category}:
    put:
      tags: [InventoryNumbers]
      parameters:
        - name: category
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InventoryNumberSchemeInput"
      responses:
        "200":
          description: Inventory number scheme updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryNumberScheme"
        "400":
          description: Invalid inventory number scheme
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Inventory number scheme not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /master-data/export:
    get:
      tags: [MasterData]
      summary: Export master data only
      responses:
        "200":
          description: Master data JSON containing entries and relations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MasterDataDocument"
        "403":
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /master-data/import:
    post:
      tags: [MasterData]
      summary: Replace local master data from a master data export
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Master data import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MasterDataImportResult"
        "400":
          description: Invalid master data file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "403":
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /master-data/{type}:
    get:
      tags: [MasterData]
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
        - name: active
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: Master data entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MasterDataEntry"
    post:
      tags: [MasterData]
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MasterDataInput"
      responses:
        "201":
          description: Master data entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MasterDataEntry"
  /master-data/{type}/{key}:
    put:
      tags: [MasterData]
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MasterDataInput"
      responses:
        "200":
          description: Master data entry updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MasterDataEntry"
        "404":
          description: Master data entry not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      tags: [MasterData]
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Master data entry deleted
        "404":
          description: Master data entry not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /master-data-relations:
    get:
      tags: [MasterData]
      parameters:
        - name: parentType
          in: query
          required: true
          schema:
            type: string
        - name: childType
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Master data relations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MasterDataRelation"
  /article-search:
    post:
      tags: [ArticleSearch]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ArticleSearchInput"
      responses:
        "200":
          description: Article search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArticleSearchResponse"
        "400":
          description: Invalid search request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "504":
          description: Search timeout or upstream failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /backup/export:
    get:
      tags: [Backup]
      summary: Export a local RailKeeper backup
      responses:
        "200":
          description: Backup JSON containing app data and upload files
          content:
            application/json:
              schema:
                type: object
        "403":
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /backup/restore:
    post:
      tags: [Backup]
      summary: Restore a local RailKeeper backup
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Restore summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupImportResult"
        "400":
          description: Invalid backup file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "403":
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
